{
  "name": "lehrunterlagen-tool",
  "private": true,
  "scripts": {
    "test": "pnpm -r test",
    "smoke": "node scripts/llm-smoke.mjs",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:dev:wsl": "WEBKIT_DISABLE_DMABUF_RENDERER=1 WEBKIT_DISABLE_COMPOSITING_MODE=1 GDK_BACKEND=x11 tauri dev",
    "tauri:build": "tauri build"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2"
  }
}
